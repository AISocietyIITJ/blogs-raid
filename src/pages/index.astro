---
import BaseHead from "../components/BaseHead.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import { SITE_TITLE, SITE_DESCRIPTION } from "../consts";
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead
      title="RAID | Realm of AI and Data"
      description="The official blog of RAID, IIT Jodhpur's AI & ML society"
    />
    <style>
      @keyframes float {
        0%,
        100% {
          transform: translateY(0px);
        }
        50% {
          transform: translateY(-20px);
        }
      }
      @keyframes glow {
        0%,
        100% {
          box-shadow: 0 0 20px rgba(59, 130, 246, 0.5);
        }
        50% {
          box-shadow: 0 0 40px rgba(59, 130, 246, 0.8);
        }
      }
      @keyframes slideInLeft {
        from {
          transform: translateX(-100px);
          opacity: 0;
        }
        to {
          transform: translateX(0);
          opacity: 1;
        }
      }
      @keyframes slideInRight {
        from {
          transform: translateX(100px);
          opacity: 0;
        }
        to {
          transform: translateX(0);
          opacity: 1;
        }
      }
      @keyframes fadeInUp {
        from {
          transform: translateY(50px);
          opacity: 0;
        }
        to {
          transform: translateY(0);
          opacity: 1;
        }
      }
      .animate-float {
        animation: float 6s ease-in-out infinite;
      }
      .animate-glow {
        animation: glow 2s ease-in-out infinite;
      }
      .animate-slide-left {
        animation: slideInLeft 1s ease-out;
      }
      .animate-slide-right {
        animation: slideInRight 1s ease-out;
      }
      .animate-fade-up {
        animation: fadeInUp 1s ease-out;
      }
      .gradient-text {
        background: linear-gradient(135deg, #030e3f 0%, #7542C7 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }
      .neural-bg {
        background-image:
          radial-gradient(
            circle at 20% 80%,
            rgba(120, 119, 198, 0.3) 0%,
            transparent 50%
          ),
          radial-gradient(
            circle at 80% 20%,
            rgba(255, 119, 198, 0.3) 0%,
            transparent 50%
          ),
          radial-gradient(
            circle at 40% 40%,
            rgba(120, 219, 255, 0.3) 0%,
            transparent 50%
          );
      }
      .card-hover:hover {
        transform: translateY(-10px);
        box-shadow: 0 25px 50px rgba(0, 0, 0, 0.25);
      }
    </style>
  </head>
  <body>
    <Header />
    <!-- <main>
      <h1>🤖 Welcome to RAID</h1>
      <p>
        This is the official blog of <strong>RAID: Realm of AI and Data</strong
        >, the Artificial Intelligence and Machine Learning society of <a
          href="https://iitj.ac.in">IIT Jodhpur</a
        >. Here, we share knowledge, projects, tutorials, and breakthroughs in
        the ever-evolving field of AI & ML.
      </p>
      <p>
        Whether you're a curious beginner or an experienced practitioner, RAID
        is a space to collaborate, learn, and lead AI innovation on campus and
        beyond.
      </p>
      <p>
        We’re excited to have you on board. Let’s decode the future together,
        one model at a time. 🚀
      </p>
    </main> -->
    <section
      class="neural-bg relative flex min-h-screen items-center justify-center overflow-hidden pt-16"
    >
      <!-- Animated Background Elements -->
      <div class="absolute inset-0 opacity-20">
        <div
          class="absolute top-20 left-10 h-2 w-2 animate-ping rounded-full bg-blue-400"
        >
        </div>
        <div
          class="absolute top-40 right-20 h-3 w-3 animate-pulse rounded-full bg-purple-400"
        >
        </div>
        <div
          class="absolute bottom-40 left-20 h-2 w-2 animate-bounce rounded-full bg-pink-400"
        >
        </div>
        <div
          class="absolute right-10 bottom-20 h-4 w-4 animate-ping rounded-full bg-cyan-400"
        >
        </div>
      </div>

      <div
        class="relative z-10 mx-auto max-w-7xl px-4 text-center sm:px-6 lg:px-8"
      >
        <div class="animate-fade-up">
          <div class="animate-float mb-8 text-8xl">🤖</div>
          <h1 class="gradient-text mb-6 text-6xl font-bold md:text-8xl">
            RAID
          </h1>
          <p class="animate-slide-left mb-4 text-2xl text-gray-300 md:text-3xl">
            Realm of AI and Data
          </p>
          <p
            class="animate-slide-right mx-auto mb-12 max-w-3xl text-lg text-gray-400 md:text-xl"
          >
            The Artificial Intelligence and Machine Learning society of IIT
            Jodhpur
          </p>

          <div
            class="flex flex-col items-center justify-center gap-6 sm:flex-row"
          >
            <a
              href={import.meta.env.BASE_URL + "/blog"}
              class="animate-glow text-white transform rounded-full bg-gradient-to-r from-blue-600 to-purple-600 px-8 py-4 text-lg font-semibold transition-all duration-300 hover:scale-105 hover:from-blue-700 hover:to-purple-700"
            >
              Explore Blogs 🚀
            </a>
            <a href ="https://www.linkedin.com/company/raid-iitj"
              target="_blank"
              rel="noopener noreferrer"
              class="rounded-full border-2 border-gray-600 px-8 py-4 text-lg font-semibold transition-all duration-300 hover:border-blue-400 hover:text-blue-400"
            >
              Join Our Community
          </a>
          </div>
        </div>
      </div>

      <!-- Scroll Indicator -->
      <!-- <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce">
            <svg class="w-6 h-6 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
            </svg>
        </div> -->
    </section>
    <Footer />
  </body>
</html>
